# Allow long report imports (GCP first-load can take hours of Mongo upserts
# after a short BigQuery job). Default consumer_timeout is 30 minutes and
# closes the delivery channel while diworker is still working.
consumer_timeout = 14400000
